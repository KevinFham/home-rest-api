GET:
  responses:
    200:
      description: 'Successfully connected with Machine Control API'
      content:
        'application/json':
          schema:
            type: 'object'
            properties:
              code:
                type: 'integer'
              message:
                type: 'string'
PUT:
  responses:
    200:
      description: 'Successfully connected with Machine Control API'
      content:
        'application/json':
          schema:
            type: 'object'
            properties:
              code:
                type: 'integer'
              message:
                type: 'string'
